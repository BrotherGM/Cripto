# Скопируйте в .env и заполните значения:  cp .env.example .env

# --- OKX API ---
# flag = "1" -> демо/тест (по умолчанию), "0" -> реальная торговля
OKX_API_KEY=
OKX_API_SECRET=
OKX_PASSPHRASE=
OKX_FLAG=1
OKX_DEBUG=false

# --- PostgreSQL ---
# Для Docker оставьте DB_HOST=db (имя сервиса в docker-compose).
# Для локального запуска используйте DB_HOST=localhost.
DB_NAME=Cripto
DB_USER=cripto
DB_PASSWORD=cripto
DB_HOST=db
DB_PORT=5432

# --- Django ---
DJANGO_SECRET_KEY=change-me-to-a-long-random-string
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=*
# Для входа в админку по домену/HTTPS укажите, напр.: https://cripto.example.com
DJANGO_CSRF_TRUSTED_ORIGINS=

# --- Автосоздание суперпользователя при старте контейнера (опционально) ---
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_PASSWORD=admin12345
DJANGO_SUPERUSER_EMAIL=admin@example.com
